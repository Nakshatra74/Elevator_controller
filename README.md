# Elevator_controller
It utilizes a Finite State Machine (FSM) to execute the standard SCAN (Elevator) algorithm, ensuring efficient directional sweeping to service multiple simultaneous floor requests. 
